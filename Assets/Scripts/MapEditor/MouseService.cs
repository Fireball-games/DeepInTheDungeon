using UnityEngine;

namespace Scripts.MapEditor
{
    public class MouseService : MonoBehaviour
    {
        private void Update()
        {
            // Vector3 worldPosition = CameraManager.Instance.MainCamera.po
        }
    }
}
